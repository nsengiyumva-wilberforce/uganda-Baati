import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uganda_baati',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
